// Compatibility exports; shared mechanics live in the public Source SDK.
export { type RecordState, type RecordScope, type RecordValue, type RecordSnapshot, digest, json, today, visibleRecord, recordScope, validateRecord, RecordStore, reviseRecord, newRecord } from 'dsh-mnemon/source-sdk'
