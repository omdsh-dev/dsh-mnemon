---
"dsh-mnemon": minor
"dsh-mnemon-source-playbooks": patch
"dsh-mnemon-source-canvas": patch
"dsh-mnemon-source-notifications": patch
"dsh-mnemon-source-sync": patch
"dsh-mnemon-source-sessions": patch
---

Share DSH theme tokens and native Markdown editing through the browser SDK. Add preview, unsaved indicators and keyboard save to notes and skill files, preserve draft versions, and discard late responses across workspaces. Align custom canvas, notification and synchronization layouts with the host theme.

Create skills in registered directories without overwriting files. Remove obsolete skill files from discovery while preserving a recoverable copy, with file-version checks and serialized writes.
