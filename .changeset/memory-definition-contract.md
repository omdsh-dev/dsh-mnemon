---
"dsh-mnemon": patch
---

Centralize optional Strategy configuration validation in the existing author helper, preserving Host discovery isolation, configuration defaults, and Loader ownership checks.
