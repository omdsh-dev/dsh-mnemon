---
"dsh-mnemon": patch
"dsh-mnemon-source-memory-spaces": patch
---

Use the Memory Spaces public configuration resolvers for Starter settings, keeping existing defaults and legacy configuration semantics while eliminating duplicate rules.
